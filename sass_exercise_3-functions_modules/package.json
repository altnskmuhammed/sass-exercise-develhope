{
  "name": "sass_exercise_3-functions_modules",
  "version": "1.0.0",
  "description": "Modify the exercise 2 code about mixin by creating a module for all declared variables.\r Then, create a module with a function. This function should accept a number as parameter and return the value converted into rem. 1rem is equal to 16px\r This function should be applied in the main stylesheet, so that the only sizing unit is rem.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch style.scss:style.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rem": "^0.7.0-3",
    "sass": "^1.54.9"
  }
}
